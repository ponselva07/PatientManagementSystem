import { PatientfilterPipe } from './patientfilter.pipe';

describe('PatientfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PatientfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
